# MES字幕助手使用说明

## 软件用途

本软件用于处理中文字幕 SRT 文件，自动完成中文优化、英文翻译和英文字幕可读性优化。

## 使用步骤

1. 启动软件后，在浏览器打开 `http://localhost:15000`。
2. 上传中文 `.srt` 字幕文件。
3. 等待系统完成中文优化。系统会修正明显的听写错误、术语和病句，并删除不承载信息的口头填充词。
4. 在校对页面确认或修改中文字幕。
5. 点击“确认并开始翻译”。
6. 下载字幕文件。

## 交付文件

每个任务会提供以下三份文件：

| 文件 | 用途 |
| --- | --- |
| `中文_<文件名>.srt` | 优化并校对后的中文字幕 |
| `英文_<文件名>.srt` | 与中文逐条对齐的英文字幕，用于逐条核对 |
| `英文_可读优化版_<文件名>.srt` | 推荐交付。系统会合并英文碎片，形成更易阅读的完整英文句子 |

## 启动软件

### Windows

双击 `00_Server/start_server.bat`，然后在浏览器打开 `http://localhost:15000`。

### macOS

首次使用请在终端进入项目根目录，执行：

```bash
chmod +x 00_Server/start_server.sh
./00_Server/start_server.sh
```

然后在浏览器打开 `http://localhost:15000`。

## 首次配置

项目根目录中需要有 `.env` 文件。将 `.env.example` 复制为 `.env`，并填写：

```text
DeepSeek_Key=你的DeepSeek密钥
```

请不要把 `.env` 文件发送给其他人，也不要上传到 GitHub。字幕文件保留在本机，但字幕文本会发送到 DeepSeek 完成 AI 处理。

## 内置术语库

软件默认使用：[TreeMES_MES_Glossary.md](00_Server/data/glossary/TreeMES_MES_Glossary.md)。

可在软件顶部点击“MES术语库”直接搜索、新增、编辑或删除术语。修改会立即写入项目中的术语库文件，后续新任务会使用最新术语。若需要同步给其他同事，由维护人员将该文件的修改通过 Git 提交，其他同事再拉取更新。

也可以直接编辑表格中的 `中文术语` 和 `推荐英文` 两列。编辑文件后，下一次上传任务会使用新术语。
