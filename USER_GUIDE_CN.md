# MES字幕助手使用说明

## 软件用途

本软件用于处理中文字幕 SRT 文件，自动完成中文优化、人工校对、目标语言翻译和目标语言字幕可读性优化。

## 使用步骤

1. 启动软件后，在浏览器打开 `http://localhost:15000`。
2. 首次使用时，在首页输入并保存 DeepSeek API Key。
3. 选择一种或多种目标语言，然后上传中文 `.srt` 字幕文件。
4. 等待系统完成中文优化。系统会修正明显的听写错误、术语和病句，并删除不承载信息的口头填充词。
5. 在校对页面确认或修改中文字幕。
6. 点击“确认并开始翻译”。
7. 下载字幕文件。

## 交付文件

中文字幕只生成一份，每种目标语言生成两份文件。总文件数为 `1 + 目标语言数 × 2`：

| 文件 | 用途 |
| --- | --- |
| `中文_<文件名>.srt` | 优化并校对后的中文字幕 |
| `<目标语言>_<文件名>.srt` | 与中文逐条对齐的目标语言字幕，用于核对 |
| `<目标语言>_可读优化版_<文件名>.srt` | 推荐交付。系统会合并目标语言碎片，形成更易阅读的完整句子 |

可选目标语言：英文、台湾繁体中文、越南语、阿拉伯语（现代标准阿拉伯语）、西班牙语（墨西哥）。目标语言支持多选，系统会逐种处理，选择的语言越多，处理时间和 API 消耗越高。

本期多语言翻译使用已经人工确认的中文内容作为源文本，并以现有中文-英文 MES 术语库作为专业标准。台湾繁体中文由 AI 按台湾用词和标点本地化；越南语、阿拉伯语和西班牙语（墨西哥）由 AI 参考标准英文术语生成，不需要营销人员逐条人工维护这些语言的术语库。

阿拉伯语使用现代标准阿拉伯语。SRT 文件可以正常保存阿拉伯文字，但导入视频剪辑软件后，需要确认软件支持从右到左排版、阿拉伯语字体和正确的字幕对齐方式。

## 启动软件

### Windows

双击项目根目录的 `启动MES字幕助手.bat`。首次启动会自动创建本机运行环境、安装依赖和打开浏览器。浏览器打开后在首页配置 DeepSeek API Key，后续直接双击同一个文件即可。

### macOS

双击项目根目录的 `启动MES字幕助手.command`。首次启动会自动创建本机运行环境、安装依赖和打开浏览器。浏览器打开后在首页配置 DeepSeek API Key。

如果 macOS 首次阻止打开该文件，请在 Finder 中按住 Control 点击文件，选择“打开”，再确认一次。之后可正常双击启动。

## 首次配置

在首页输入 DeepSeek API Key 后，软件会在项目根目录自动创建或更新 `.env`：

```text
DeepSeek_Key=你的DeepSeek密钥
```

请不要把 `.env` 文件发送给其他人，也不要上传到 GitHub。字幕文件保留在本机，但字幕文本会发送到 DeepSeek 完成 AI 处理。

网页只显示 API Key 的前 3 位和后 4 位，例如 `sk-******1234`。完整密钥不会由后端返回给页面；需要更换时，直接输入新密钥并保存即可。

如果启动器提示没有 Python 3.10 或更高版本，直接将提示内容发送给 Codex，请它协助安装后再重新双击启动器。

## 内置术语库

软件默认使用：[TreeMES_MES_Glossary.md](00_Server/data/glossary/TreeMES_MES_Glossary.md)。术语库以中文-英文为标准，其他语言由 AI 参考这套标准生成，不要求营销人员额外维护多语言术语。

可在软件顶部点击“MES术语库”直接搜索、新增、编辑或删除术语。修改会立即写入项目中的术语库文件，后续新任务会使用最新术语。若需要同步给其他同事，由维护人员将该文件的修改通过 Git 提交，其他同事再拉取更新。

也可以直接编辑表格中的 `中文术语` 和 `推荐英文` 两列。编辑文件后，下一次上传任务会使用新术语。
